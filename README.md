# Реестр клеток — Майами

Карта города, где место на карте одновременно является рекламным местом.
Полное ТЗ: `docs/TZ.md`.

## Стек

Next.js (App Router) · PostgreSQL + PostGIS · Prisma · MapLibre GL JS + OpenStreetMap · Stripe

## Быстрый старт (локально)

```bash
npm install
cp .env.example .env       # заполнить DATABASE_URL, STRIPE_*
npx prisma migrate deploy
npm run db:seed
npm run dev
```

Открыть http://localhost:3000 — карта Майами. Двойной тап по карте открывает форму
занятия клетки.

## Деплой (Vercel + Neon)

`npm run build` сам прогоняет `prisma generate && prisma migrate deploy &&
tsx prisma/seed.ts && next build` — то есть миграции и сид категорий
применяются автоматически при каждом деплое на Vercel, вручную ничего
накатывать не нужно. Единственное требование — переменная `DATABASE_URL`
должна быть задана в настройках проекта на Vercel (Neon connection string).

## Структура

- `prisma/schema.prisma` — модель данных (раздел 7 ТЗ)
- `prisma/migrations/` — включая правило «одна клетка — одна услуга — один владелец» (частичный unique индекс) на уровне БД
- `src/lib/grid.ts` — математика сетки клеток (раздел 8 ТЗ)
- `src/components/MapView.tsx` — карта, два слоя, сетка
- `src/app/b/[slug]` — публичная карточка бизнеса (SSR, замена сайта)
- `src/app/claim/new` — регистрация по телефону + занятие клетки + Stripe checkout
- `src/app/dashboard` — кабинет владельца, счётчики показов/звонков

## Объём первой версии (раздел 13 ТЗ)

Сделано в каркасе: карта с сеткой и двумя слоями, регистрация по телефону (стаб
OTP), занятие клетки с уникальностью категории на уровне БД, публичная карточка
с SSR, список «рядом со мной», бейдж «новый», счётчик показов/нажатий, оплата
через Stripe.

Не в первой версии (сознательно): отзывы, мобильные приложения, чат, авто-сверка
с реестром юрлиц (Sunbiz) — сверка ручная на старте.

## TODO перед продакшеном

- Реальный SMS OTP провайдер вместо заглушки в `src/app/api/auth/*`
- Ручная/автоматическая сверка со Sunbiz (Florida Division of Corporations) для галочки «Проверено»
- Настоящая авторизация/сессии вместо `ownerId` в query у `/dashboard`
- Возврат оплаты при конфликте клетки (`src/app/api/stripe/webhook/route.ts`)
- MapTiler/Protomaps тайлы вместо публичного OSM tile-сервера (не для продакшен-нагрузки)
